using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class destroyOnAnim : MonoBehaviour
{
    // Start is called before the first frame update
public void destroyInAnim()
    {
        Destroy(gameObject);
    }
}
